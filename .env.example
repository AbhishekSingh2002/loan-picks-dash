# =============================================================================
# WHY: Environment variables template
# Copy to .env.local for development
# Never commit actual .env files to Git (security)
# =============================================================================

# Database Configuration
# WHY: Postgres connection string for Prisma or Supabase
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@localhost:5432/loan_picks_db"

# Supabase (Alternative to Prisma + Postgres)
# WHY: If using Supabase, these provide client access
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# AI Provider API Keys
# WHY: For production AI grounding (lib/ai.ts callLLMAPI function)
# Choose either OpenAI or Gemini
OPENAI_API_KEY="sk-your-openai-key-here"
GEMINI_API_KEY="your-gemini-key-here"

# Authentication (NextAuth.js)
# WHY: For user authentication and session management
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-random-secret-here"

# OAuth Providers (Optional)
# WHY: For social login (Google, GitHub, etc.)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# JWT Configuration
# WHY: For securing API endpoints
JWT_SECRET="another-random-secret-for-jwt"

# Rate Limiting (Redis)
# WHY: For rate limiting AI chat endpoint
REDIS_URL="redis://localhost:6379"

# App Configuration
# WHY: App-level settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"

# =============================================================================
# Security Notes:
# 1. Generate strong random secrets: openssl rand -base64 32
# 2. Never commit .env files to version control
# 3. Use different values for production vs development
# 4. Rotate secrets regularly
# 5. Use environment variables in deployment platform (Vercel, etc.)
# =============================================================================